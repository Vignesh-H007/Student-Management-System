import tkinter as tk
root = tk.Tk()
root.title("Student Management System")

label = tk.Label(root, text="Welcome to the Student Management System")
label.grid(row=0, column=0, padx=10, pady=10)

root.mainloop()